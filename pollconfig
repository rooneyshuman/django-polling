server {
    listen 80;
    server_name srebestgroup2.ipq.co;
    location /static/ {
        root /var/www/polling;
    }
    location = /favicon.ico {access_log off; log_not_found off;}
    location / {
        include proxy_params;
        proxy_pass http://localhost:8000;
    }
}
